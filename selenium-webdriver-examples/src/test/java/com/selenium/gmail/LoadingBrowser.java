package com.selenium.gmail;

import org.openqa.selenium.WebDriver;
import org.testng.annotations.Parameters;

public class LoadingBrowser {

	WebDriver driver;

	@Parameters
	public void Test(String browser) {

		//
		// if(browser.equalIgnoreCase (i.e));
		// webDriver driver= new i.e driver;
		//
		// else if(browser.equalIgnoreCase(firefox));
		// webDriver driver= new firefox driver;
		//
		// else if(browser.equalIgnoreCase(chrome));
		// webDriver driver= new chrome driver;
		//
		// }
		//
		//
		// System.setproperties(WebDriver.geckodriver.driver,pathfile);
		// driver.get("url");
	}
}